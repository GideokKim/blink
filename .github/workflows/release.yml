name: Release

# Cut a release by pushing a version tag:  git tag v1.2.3 && git push origin v1.2.3
# (or run manually from the Actions tab with a version). Builds the installer on Windows
# and publishes a GitHub Release with auto-generated notes and the Setup .exe attached.

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version without leading v (e.g. 1.2.3 or 0.0.1-test)'
        required: true
      prerelease:
        description: 'Mark as pre-release'
        type: boolean
        default: false

permissions:
  contents: write   # required to create the release and upload assets

jobs:
  release:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
    # Code signing turns on automatically once the SignPath secret is configured.
    # Until then every signing step below is skipped and the release is published
    # unsigned exactly as before — so this is safe to merge before SignPath is set up.
    # See RELEASING.md → "Code signing (SignPath)" for the one-time setup.
    env:
      SIGNING_ENABLED: ${{ secrets.SIGNPATH_API_TOKEN != '' }}

    steps:
      # fetch-depth: 0 pulls full history + tags so the notes step can diff against the
      # previous tag (git log <prev>..HEAD) to build the auto-generated detail section.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '8.0.x'

      - name: Resolve version
        id: ver
        run: |
          if ($env:GITHUB_REF -like 'refs/tags/*') {
            $v = $env:GITHUB_REF -replace 'refs/tags/v?',''
          } else {
            $v = '${{ github.event.inputs.version }}'
          }
          if (-not $v) { throw 'Could not resolve a version' }
          # Pre-release when the version carries a semver pre-release suffix (e.g. 0.0.1-test,
          # 1.2.0-rc1, -beta, -alpha), or when the manual run requests it.
          $pre = if ($v -match '-') { 'true' } else { 'false' }
          if ('${{ github.event.inputs.prerelease }}' -eq 'true') { $pre = 'true' }
          "version=$v"      >> $env:GITHUB_OUTPUT
          "tag=v$v"         >> $env:GITHUB_OUTPUT
          "prerelease=$pre" >> $env:GITHUB_OUTPUT
          Write-Host "Building version $v (prerelease=$pre)"

      - name: Test engine
        run: dotnet test Blink.Core.Tests -c Release

      # -p:Version stamps AssemblyInformationalVersion so the app knows its own version
      # (the in-app updater compares it against the latest release tag).
      - name: Publish app (self-contained, single file)
        run: >
          dotnet publish Blink.App -c Release -r win-x64 --self-contained
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -p:Version=${{ steps.ver.outputs.version }}

      - name: Publish indexer worker (self-contained, single file)
        run: >
          dotnet publish Blink.Indexer.Worker -c Release -r win-x64 --self-contained
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -p:Version=${{ steps.ver.outputs.version }}

      # --- Sign the app + worker exes BEFORE packaging (so the installed binaries
      #     are signed too). Inner files can't be signed after Inno Setup bundles them.
      #     All three signing steps no-op unless SIGNING_ENABLED is true. ---
      - name: Stage binaries for signing
        if: env.SIGNING_ENABLED == 'true'
        run: |
          New-Item -ItemType Directory -Force sign-in/binaries | Out-Null
          Copy-Item Blink.App/bin/Release/net8.0-windows/win-x64/publish/Blink.App.exe        sign-in/binaries/
          Copy-Item Blink.Indexer.Worker/bin/Release/net8.0/win-x64/publish/Blink.Indexer.Worker.exe sign-in/binaries/

      - name: Upload unsigned binaries
        if: env.SIGNING_ENABLED == 'true'
        id: artifact-binaries
        uses: actions/upload-artifact@v7
        with:
          name: unsigned-binaries
          path: sign-in/binaries

      - name: Sign binaries (SignPath)
        if: env.SIGNING_ENABLED == 'true'
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ vars.SIGNPATH_ORGANIZATION_ID }}
          # These three slugs must match what you define in the SignPath portal.
          project-slug: blink
          signing-policy-slug: release-signing
          artifact-configuration-slug: binaries
          github-artifact-id: ${{ steps.artifact-binaries.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: sign-out/binaries

      - name: Copy signed binaries back into publish output
        if: env.SIGNING_ENABLED == 'true'
        run: |
          Copy-Item sign-out/binaries/Blink.App.exe            Blink.App/bin/Release/net8.0-windows/win-x64/publish/ -Force
          Copy-Item sign-out/binaries/Blink.Indexer.Worker.exe Blink.Indexer.Worker/bin/Release/net8.0/win-x64/publish/ -Force

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Build installer
        run: >
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          /DAppVersion=${{ steps.ver.outputs.version }}
          installer\blink.iss

      # --- Sign the final installer. Output overwrites the unsigned Setup.exe in
      #     installer/Output, so the release upload step below needs no changes. ---
      - name: Upload unsigned installer
        if: env.SIGNING_ENABLED == 'true'
        id: artifact-installer
        uses: actions/upload-artifact@v7
        with:
          name: unsigned-installer
          path: installer/Output/Blink-Setup-*.exe

      - name: Sign installer (SignPath)
        if: env.SIGNING_ENABLED == 'true'
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ vars.SIGNPATH_ORGANIZATION_ID }}
          project-slug: blink
          signing-policy-slug: release-signing
          artifact-configuration-slug: installer
          github-artifact-id: ${{ steps.artifact-installer.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: installer/Output

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: Blink ${{ steps.ver.outputs.tag }}
          # Notes are composed by the next step (hybrid: hand-written highlights from
          # CHANGELOG.md + commit-derived detail). The release is created with an empty
          # body here, then `gh release edit` fills it in.
          generate_release_notes: false
          prerelease: ${{ steps.ver.outputs.prerelease }}
          files: installer/Output/Blink-Setup-*.exe

      # Compose hybrid release notes, then publish the in-app update manifest (CDN-served,
      # avoids the REST API rate limit).
      #
      #   highlights  — hand-written, user-facing, from CHANGELOG.md (the single source of
      #                 truth for "what's new"). This is ALL the in-app card shows.
      #   detail      — auto-generated from commits since the previous tag, grouped by
      #                 Conventional Commit type. Captures direct-to-main commits too (no PR
      #                 needed), and goes in a collapsible block on the web release page only.
      - name: Compose release notes + update manifest
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag  = '${{ steps.ver.outputs.tag }}'
          $ver  = '${{ steps.ver.outputs.version }}'
          $repo = '${{ github.repository }}'

          # --- 1) Highlights: read the CHANGELOG.md section for this version. Falls back to
          #        [Unreleased] if the section hasn't been stamped with the version yet. ---
          function Get-Changelog([string]$path, [string]$version) {
            if (-not (Test-Path $path)) { return '' }
            $lines = Get-Content -Path $path -Encoding utf8
            $start = -1
            for ($i = 0; $i -lt $lines.Count; $i++) {
              if ($lines[$i] -match "^##\s*\[$([regex]::Escape($version))\]") { $start = $i; break }
            }
            if ($start -lt 0) {
              for ($i = 0; $i -lt $lines.Count; $i++) {
                if ($lines[$i] -match '^##\s*\[Unreleased\]') { $start = $i; break }
              }
            }
            if ($start -lt 0) { return '' }
            $out = @()
            for ($i = $start + 1; $i -lt $lines.Count; $i++) {
              if ($lines[$i] -match '^##\s') { break }
              $out += $lines[$i]
            }
            return (($out -join "`n").Trim())
          }
          $highlights = Get-Changelog 'CHANGELOG.md' $ver

          # --- 2) Detail: group commits since the previous tag by Conventional Commit type.
          #        --no-merges drops the umbrella `merge:` commits, leaving the typed leaves. ---
          $prev  = (git describe --tags --abbrev=0 HEAD^ 2>$null)
          $range = if ($prev) { "$prev..HEAD" } else { 'HEAD' }
          $subjects = @(git log $range --no-merges --pretty=format:'%s')
          $groups = [ordered]@{
            '✨ 새 기능' = @(); '🐛 버그 수정' = @(); '⚡ 성능' = @(); '📝 문서' = @(); '🔧 기타 변경' = @()
          }
          foreach ($s in $subjects) {
            if (-not $s) { continue }
            if ($s -match '^(\w+)(\([^)]*\))?!?:\s*(.+)$') {
              $type  = $matches[1].ToLower()
              $scope = if ($matches[2]) { $matches[2].Trim('(', ')') + ': ' } else { '' }
              $desc  = $matches[3].Trim()
              $cat = switch ($type) {
                'feat' { '✨ 새 기능' } 'fix' { '🐛 버그 수정' } 'perf' { '⚡ 성능' }
                'docs' { '📝 문서' } default { '🔧 기타 변경' }
              }
              $groups[$cat] += "- $scope$desc"
            } else {
              $groups['🔧 기타 변경'] += "- $s"
            }
          }
          $detailParts = @()
          foreach ($k in $groups.Keys) {
            if ($groups[$k].Count -gt 0) { $detailParts += "#### $k`n" + ($groups[$k] -join "`n") }
          }
          $detail = ($detailParts -join "`n`n").Trim()
          if ($prev) { $detail = ($detail + "`n`n**전체 변경 내역**: https://github.com/$repo/compare/$prev...$tag").Trim() }

          # --- 3) latest.json — read by the in-app updater via the stable CDN URL
          #        https://github.com/<repo>/releases/latest/download/latest.json (not the
          #        60/h REST API). The app shows the user-facing highlights only; if a release
          #        was tagged without a CHANGELOG entry, fall back to detail so it's never empty. ---
          $appNotes = if ($highlights) { $highlights } else { $detail }
          $installerName = "Blink-Setup-$ver.exe"
          $manifest = [ordered]@{
            version       = $ver
            tag           = $tag
            notes         = $appNotes
            installerName = $installerName
            installerUrl  = "https://github.com/$repo/releases/download/$tag/$installerName"
          }
          $manifest | ConvertTo-Json -Depth 5 | Set-Content -Path latest.json -Encoding utf8
          gh release upload $tag latest.json --clobber

          # --- 4) Web release body: highlights above the <!--BLINK_NOTES_END--> sentinel,
          #        collapsible commit detail below it. The app reads latest.json (not this
          #        body), so the sentinel is just a boundary marker on the web page here. ---
          $webParts = @()
          if ($highlights) { $webParts += $highlights }
          $webParts += '<!--BLINK_NOTES_END-->'
          if ($detail)     { $webParts += "<details>`n<summary>전체 변경 내역</summary>`n`n$detail`n`n</details>" }
          $webBody = ($webParts -join "`n`n")
          Set-Content -Path release-notes.md -Value $webBody -Encoding utf8
          gh release edit $tag --notes-file release-notes.md
